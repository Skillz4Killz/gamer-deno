import { botCache } from "../../deps.ts";

botCache.eventHandlers.raw = function (data) {
//   if (!botCache.fullyReady) return;
//   if (!data.t || ["CHANNEL_DELETE", "GUILD_MEMBER_UPDATE", "CHANNEL_UPDATE", "VOICE_STATE_UPDATE", "MESSAGE_REACTION_ADD", "MESSAGE_REACTION_REMOVE", "GUILD_MEMBER_REMOVE"].includes(data.t)) return;

//   console.log(data);
};
